# Class that installs & provides minimal configuration for the dependencies that
# Machine Zero produced. These dependencies are needed to compile the Paycasso 
# system. 

class build-dependencies {

  #package { 'boost':
  #  ensure => 'installed',
  #  source => 'http://s3.amazonws.com/paycasso/...'
  #  provider => rpm,
  #}

  #package { 'opencv':
  #  ensure => 'installed',
  #  source => 'http://s3.amazonws.com/paycasso/...'
  #  provider => rpm,
  #}

  #package { 'leptonica':
  #  ensure => 'installed',
  #  source => 'http://s3.amazonws.com/paycasso/...'
  #  provider => rpm,
  #}

  #package { 'tesseract':
  #  ensure => 'installed',
  #  source => 'http://s3.amazonws.com/paycasso/...'
  #  provider => rpm,
  #}

}

